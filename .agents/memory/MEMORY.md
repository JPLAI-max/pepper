- [Pepper auth & ownership model](pepper-single-user.md) — multi-user cookie sessions; identity from req.session.userId only; conversation + goals/roadmap/documents scoped by server-owned userId; opportunities stays global. No IDOR exemption.
- [Pepper trust gate & continuity](pepper-auth-trust-gate.md) — anon chat → auth-required SSE → signup links anon conversation via session.conversationId; connect-pg-simple session table defined in Drizzle so push provisions it.
- [Pepper voice/SSE contract](pepper-voice-sse.md) — text vs voice SSE event shapes differ; voice returns full mp3 in one event (no worklet).
- [Pepper guest-transcript auto-purge](pepper-guest-cleanup.md) — hourly job deletes unclaimed (userId IS NULL) convos older than GUEST_RETENTION_HOURS (def 48); claimed convos never touched; messages go via FK cascade.
- [Pepper redesign constraints](pepper-redesign-constraints.md) — restyling must NOT touch src/pepper/ or break Documents @dnd-kit; use two-phase reference-then-match design delegation.
- [Pepper coach prompt vs JSON extraction](pepper-coach-prompt.md) — coach spec is a natural-language system prompt; don't force the spec's per-turn JSON output (streaming chat would show raw JSON).
- [Pepper silent extraction](pepper-silent-extraction.md) — after-turn JSON-only OpenAI pass, never shown in chat; persists to profile+history; aggregate-overwrite risk mitigated via carry-forward prompt.
- [api-zod/client codegen is spec-derived](api-codegen-drift.md) — generated files must match openapi.yaml; never hand-edit them. Types in generated output but absent from the spec get wiped by any codegen, breaking dependents.
- [Pepper readiness scoring engine](pepper-readiness-scoring.md) — deterministic no-AI engine; presence = field ∈ profiles.capturedFields (NOT value>0), so captured 0 is real & uncaptured stays unknown/partial; /scores merges legacy+engine fields additively.
- [Roadmap & scores grounding](roadmap-and-scores-engine.md) — GET endpoints read-only with in-memory fallback; persistence only on the extraction recompute path; roadmap copy may only use stored/derived figures, else go non-numeric. Roadmap is engine-owned: deterministic stableKey reconcile preserves status; PATCH is status-only.
- [Opportunity matcher rules](pepper-opportunity-matcher.md) — deterministic educational category matcher (GET /opportunities/matches); never "you qualify"/invented numbers; investment/income-units/margin only ever "coming_later", never flipped to "available" by a flag.
- [Running api-server ESM standalone](esm-api-server-standalone-scripts.md) — to exercise api-server functions in a one-off, bundle a temp entry inside src/ with esbuild (externalize pg-native/*.node), not tsx/scripts.
- [Reveal screen](reveal-screen.md) — iframe-embedded static page; parent must override CTA onclick for SPA nav; readyForReveal needs profile-query invalidation after each turn; overwrite/hide ALL sample binds.
- [Pepper git sync & push](pepper-git-sync-push.md) — "reset --hard origin/main" destroys local-ahead commits (origin is often behind); check direction first, fast-forward push instead. Clear stale .git lock via code_execution fs; commit+push via code_execution child_process with github token.
- [Dashboard engine integration](dashboard-engine-integration.md) — dashboard sources scores/roadmap/opportunities from their own GET endpoints (no re-derive); money fields gate on capturedFields → null (UI hides, never $0); coach turns invalidate all engine surfaces.
- [Hey Pep overlay navigation + tour](pepper-overlay-navigation.md) — overlay coach navigates by name AND runs a guided tour (/market→/financing→/capital-markets); server-owned allowlist + TOUR_STOPS; banner is global TourBanner (overlay hidden on takeover routes); reset() must clear tour.
- [Hey Pep overlay (coach Mode B)](pepper-overlay-mode-b.md) — overlay reuses usePepper; sendText {mode,section,commit}; only commit===true runs extractAndPersist (awaited). Every overlay turn STILL persists its message (extraction reads whole history); "no writes" = no financial-data writes. Onboarding keeps PepperAssistant, not the overlay.
- [Pepper overlay navigation (Mode B)](pepper-overlay-navigation.md) — voice/typed "take me to X" → server allowlisted route (6-route subset). Gated separate JSON classifier (not streamed reply, avoids JSON-in-chat); raw SSE {navigate} before {done}; sendText returns {navigate}; overlay mic is dictation→text route so both paths covered by handle().
- [Pepper takeover/simulation routes](pepper-takeover-routes.md) — full-screen iframe pages (/reveal,/market,/financing,/capital-markets) need sandbox=allow-scripts (no allow-same-origin) + APP_SHELL_ROUTES + AppLayout isTakeover, or real engines leak in.
- [Pepper document upload + parsing](pepper-document-upload.md) — chat doc ingest: never-annualize on doc path (mapToProfileFields opts), ingest owner pre-check before ACL/delete, confirm-only write via shared persistProfileFields.
- [Pepper iframe cookie + CSRF](pepper-iframe-cookies-csrf.md) — session cookie MUST be SameSite=None;Secure (cross-site preview iframe) or session is lost between requests (403); compensate with csrfOriginGuard Origin allowlist on mutating /api. Test via HTTPS domain, not localhost.
